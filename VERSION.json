{
  "version": "2.1.0",
  "app_name": "Harvest Hero Inventory Tracker",
  "last_updated": "2026-08-25T00:00:00",
  "features": [
    "Role-based access control",
    "Weight/pounds tracking with monthly cadence",
    "Customizable reporting and exports",
    "Barcode management with bulk import",
    "Shelf and overflow management with edit/save",
    "Shopping list auto-population",
    "Windows installer with in-app upgrade",
    "Persistent user data across every install and update"
  ],
  "new_in_2_1_0": [
    "Windows clients now install from a real installer (HarvestHeroSetup-<ver>.exe) built by GitHub Actions on every tag. No Python required on the client machine.",
    "Installer registers the app with Windows so it appears in Settings > Apps, gets Start-menu and desktop shortcuts, and can be uninstalled cleanly without touching user data.",
    "In-app updater now downloads the installer, verifies its SHA-256 against the release's .sha256 sidecar, and hands off to the installer with silent-close-restart flags. No file-copy race conditions.",
    "Broken download or checksum mismatch leaves the current install untouched -- the client can retry when ready.",
    "Release process: developer tags v<version> on macOS, GitHub Actions builds the Windows installer, GitHub Release is created automatically, and every already-installed client offers the update on next launch."
  ],
  "recent_fixes": [
    "Update flow no longer relies on overwriting files in the running app folder.",
    "Semantic version (major.minor.patch) is now the source of truth for both the installer and the exe file metadata.",
    "Old broken release.yml workflow removed and replaced with windows-release.yml."
  ],
  "deployment": {
    "user_data_dir_windows": "%APPDATA%\\HarvestHero",
    "install_dir_windows": "%LOCALAPPDATA%\\Programs\\HarvestHero",
    "user_data_dir_macos": "~/Library/Application Support/HarvestHero",
    "user_data_dir_linux": "~/.local/share/HarvestHero",
    "update_method": "GitHub Releases + Inno Setup installer with SHA-256 verification",
    "update_frequency": "Check on app startup",
    "data_preservation": "User data lives outside the install directory; the installer never writes to it."
  }
}
